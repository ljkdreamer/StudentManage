<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD 
    Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljk.programmer.dao.ClazzDao">

  

  <insert id="add" parameterType="Clazz" >
     insert into clazz(id,gradeId ,name,remark) values(null,#{gradeId},#{name},#{remark})  
  </insert>      

<select id="findList" parameterType="Map"  resultType="Clazz">

  select *  from clazz 
  where 1=1
  <if test="name!=null"> and  name like #{name}</if>
   <if test="gradeId!=null"> and  gradeId   like #{gradeId}</if>
  
  
   limit #{offset},#{pageSize}

</select>


  <select id="getTotal"  parameterType="Map" resultType="Integer">
     select count(id)  from clazz  where 1=1
     <if test="name!=null">and  name like #{name}</if>
     <if test="gradeId!=null"> and  gradeId   like #{gradeId}</if>
  </select>


  <update id="eidt" parameterType="Clazz"  >
  
     update clazz SET  gradeId=#{gradeId}, name=#{name},
     remark=#{remark} where id=#{id}
  </update>
  
  <delete id="delete"  >
        
     delete from clazz where 
     id in ${value}
  
  </delete>
  
  <select id="findAll"  resultType="Clazz">

  select *  from clazz 
  

</select>


</mapper>